
# Dependencies
node_modules/

# Build output (tsc → outDir)
dist/

# Environment variables
.env
.env.local

# Généré depuis ../docs/openapi.yaml — recréé par le hook `prepare`.
src/domain/api.generated.ts

# Généré par kysely-codegen depuis la base migrée — recréé par le hook `prepare`.
src/store/schema.generated.ts
